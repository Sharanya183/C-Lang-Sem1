#include <stdio.h>

int main() {
   
    int n, i, res=0;
    
    printf("enter n: ");
    scanf("%d", &n);
    
    for(i=0; i<=n; i++){
        res = res + i;
    }
    
    printf("sum of %d natural numbers is %d", n, res);
    
    return 0;

}
